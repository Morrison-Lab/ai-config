# Session notebook - 2026-08-22 - gi #1953 (all-open-PRs monitor dead)

Working repo: Morrison-Lab/ai-config. Task: `gi` grabbed highest-priority
issue, implement, PR, ARDI clean. User asked mid-flight whether gi has a
worktree step (it does not; AGENTS.md mandates isolation anyway - gap issue
to be filed), then said "drive it to clean".

## State

- Issue claimed: [#1953](https://github.com/Morrison-Lab/ai-config/issues/1953)
  (claim comment 5382086200).
- Worktree: `C:\Users\dougm\Documents\Github\ai-config-pr1953`
  (branch `fix/all-open-prs-monitor-gh-path`, off origin/main @ 11dc0162).
  Main checkout sits on someone else's branch
  (`feat/cursor-first-class-consumer`) - untouched by design.
- Draft PR opened up front: [#1976](https://github.com/Morrison-Lab/ai-config/pull/1976),
  body carries `Closes #1953`.
- Review trigger: ai-config auto-reviews on push since #1707 (2026-08-20);
  do NOT also dispatch workflow_dispatch or post "@claude review" (race).

## Fix shape (three parts, per issue proposal)

1. install-pr-monitor.py: resolve gh/python3 absolutely at install, fail
   fast if missing; persist interactive PATH via cron line
   (`/usr/bin/env PATH="..."`) + systemd drop-in
   `ai-config-pr-monitor.service.d/10-path.conf`.
2. monitor-open-prs.py: GH_PATH resolved at import, require_gh() exits
   non-zero at startup instead of error-looping; poll_once() split out;
   error_streak counter reset on success.
3. inject-pr-monitor-status.py: PERSISTENT_ERROR_POLLS=3; surfaces an
   unchanged persistent error once, flags it
   (`persistent_error_reported`), clears on recovery.

## Outcome

MERGED. PR #1976 squash-merged as 74ed69d5 at 2026-08-24T01:40:31Z;
issue #1953 auto-closed COMPLETED one second later.

A parallel session took over this same branch+worktree while the
originating session was blocked on local checks and user interrupts:
it committed the implementation (2f96fd13), synced main, ran four
review-round commits (adversarial self-review + verify-round findings),
and drove #1976 to merge. The originating session's stray duplicate
commit df0ff5f8 (its pre-review rewrite of test-inject) is superseded --
the merged tests cover the full persistent-error lifecycle plus extra
edges -- and was discarded unpushed.

## Log

- [x] Claimed, worktree cut, draft PR open.
- [x] All five files implemented (monitor, injector, installer, 2 tests).
- [x] Local checks blocked twice by opencode Bash tool failure
      (Unknown: ChildProcess.kill) and once by user interrupt; filed as
      ai-config#2077; CI validated instead (user's explicit call).
- [x] Commit/push/ARDI/merge completed via parallel-session takeover
      (see Outcome above).
- [x] Post-merge tidy: worktree ai-config-pr1953 removed; local branch
      had already been deleted by the other session.
- [ ] Host action owed: re-run scripts/install-pr-monitor.py on the
      machine running the stale cron/daemon so it picks up the fixed
      install (repo-side fix does not repair the running process).
