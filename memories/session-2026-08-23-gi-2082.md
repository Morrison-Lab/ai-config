# Session 2026-08-23 - gi #2082 Windows PR-monitor backstop

## Objective
Implement Morrison-Lab/ai-config#2082: make the all-open-PRs monitor installable
on Windows as a second backstop host, via the gi flow (claim -> worktree ->
draft PR -> implement -> checks -> ready -> ARDI). Follow-on to merged #1953
(PR #1976); user approved with "do it".

## State
- Worktree C:\Users\dougm\Documents\Github\ai-config-win2082,
  branch feat/win-pr-monitor-backstop.
- Claim comment: https://github.com/Morrison-Lab/ai-config/issues/2082#issuecomment-5391201511
- Draft PR opened empty, then filled and marked READY after implementation:
  https://github.com/Morrison-Lab/ai-config/pull/2084 (Closes #2082).
- Implementation commit d5c59b33 pushed 2026-08-23 ~22:55 PT:
  - hooks/monitor-open-prs.py: alive() -> ctypes OpenProcess probe on nt;
    pid<=0 guard; POSIX keeps os.kill(pid,0).
  - scripts/install-pr-monitor.py: schtasks job ai-config-pr-monitor /MO 5;
    --status/--uninstall verbs (nt-only); resolve_dependencies falls back to
    sys.executable; stop_stale_daemon loads hook module unconditionally and
    probes module.alive(); SIGTERM-on-nt == TerminateProcess noted.
  - tests: installer suite platform-split stop assertions + GoneModule stub +
    task/verb/fallback checks (26/26 local PASS); hook test alive() cases.
  - README: Windows paragraph under install section.
- Local full runner scripts/test_hooks.py HANGS on Windows inside
  test-no-clobbering-push.py fixture build -> filed
  https://github.com/Morrison-Lab/ai-config/issues/2098 (suspects: LFS
  filter-process spawn in scratch clones; bare "python3" spawn at line 466;
  runner lacks per-suite timeout). My two touched suites pass standalone;
  Linux CI authoritative for the sweep.
- opencode Bash ChildProcess.kill bug (#2077) recurred repeatedly around
  long/timeout-killed commands; cmd /c wrapper dodges it. Consider appending
  this evidence to #2077 at wrap-up.

## Next steps
1. Poll PR #2084 head d5c59b33 for CI + auto-review (ardi loop).
2. Address findings; drive to fully-clean. Merge needs standing mwc grant for
   ai-config target (fully-clean only).
3. After merge: run scripts/install-pr-monitor.py ON THIS MACHINE for real,
   verify schtasks entry + --status, confirm first poll writes state file.
4. Mac-host reinstall still owed separately (user pivoted away; blocked on SSH route).

## Log
- 2026-08-23 ~21:47 PT: #1953 closed out (merged 74ed69d5 via parallel session);
  issues #2061 #2077 filed; notebook finalized (uncommitted in shared checkout).
- ~22:20 PT: #2082 filed; gi flow started; worktree cut off origin/main @980381dd;
  empty b89118fe pushed; draft PR #2084 opened.
- ~22:40 PT: read current sources; designed Windows approach; applied hook edit.
- ~22:50 PT: installer edits + test updates; 25/26 then fixed stub-probe SIGTERM
  patch -> 26/26 PASS.
- ~22:55 PT: test_hooks.py hang investigated -> issue #2098 filed; README note
  added; commit d5c59b33 pushed; PR marked ready.
