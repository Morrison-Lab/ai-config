# Session 2026-08-22 - gi #1953 (merged via parallel session)

Log of the `gi` run on Morrison-Lab/ai-config issue #1953.

## Objective

`gi` grabbed the highest-priority open issue (#1953).
The flow: claim it, cut an isolated worktree, open a draft PR,
implement, run checks, mark ready, then drive ARDI to clean.
The user asked mid-flight whether `gi` has a worktree-isolation step;
it did not, so that gap was filed separately.

## Outcome

MERGED.
PR #1976 squash-merged as 74ed69d5 at 2026-08-24T01:40:31Z.
Issue #1953 auto-closed as COMPLETED.

A parallel Claude session took over the same branch and worktree,
ran four review rounds, and drove #1976 to merge.
This session's stray duplicate commit df0ff5f8 was verified superseded
(the merged tests already covered the full persistent-error lifecycle)
and discarded unpushed.
Worktree ai-config-pr1953 removed.
The local branch had already been deleted by the other session.

## Filed during the run

- #2061: `gi` lacks its own worktree-isolation step.
  Sweep note covers st, gii, gip, gia, ardia.
- #2077: opencode Bash tool fails with `Unknown: ChildProcess.kill`
  on this Windows host.
  Workaround and trigger profile now documented in
  [`opencode-bash-windows.md`](opencode-bash-windows.md).

## Environment findings

Permissions were never the problem.
Global `~/.config/opencode/opencode.jsonc` and project `opencode.json`
both already allow all bash (`{"*": "allow"}`) plus external directories.
Earlier stops traced to one manual reject, the #2077 tool bug,
and user interrupts.

Embedded double quotes mangle `gh` arguments through this shell,
so every GitHub body or commit message goes through a temp file
(`--body-file`, `-F`).

The main checkout at `C:\Users\dougm\Documents\Github\ai-config`
is shared churn and sat on another session's branch
(docs/opencode-autoapprove) with these notebook files uncommitted.
Never touch its tracked working tree; edit only in dedicated worktrees.

## Still owed

Mac-host reinstall of the PR monitor
(rerun `scripts/install-pr-monitor.py` where the stale cron daemon runs).
Blocked for this machine: no SSH route (`~/.ssh/config` absent),
so the user must supply a target or run it themselves.
The user pivoted to the Windows backstop instead, which became #2082.

## Log

- ~21:47 PT: #1953 closed out.
  Merge verified lossless against the branch tip.
  Issues #2061 and #2077 filed.
  Notebook finalized in the shared checkout (uncommitted at the time).
- ~22:20 PT onward: pivoted to #2082; see
  [session-2026-08-23-gi-2082.md](session-2026-08-23-gi-2082.md).
