# MEMORY.md --- index of `memories/`

Index of the cross-project memory files in this directory. When you add a
*new* memory file, register it here so the corpus stays discoverable --- the
`memorize` skill writes the file, then adds a row to the table below.
(Appending a bullet to an existing file needs no change here.)

This index covers only the general, cross-project memories kept in the
ai-config repo. Repo-specific memories live outside the repo, under
`~/.claude/projects/<project-path>/memory/`, each with its own `MEMORY.md`
index in that directory.

| File | Title | Covers |
|------|-------|--------|
| [`preferences.md`](preferences.md) | User preferences (cross-workspace) | Standing working rules: never-assume/always-verify, record learnings as you go, cite sources for tool-behavior claims, issue-first, and the ARDI / fully-clean definitions. |
| [`reviewing-prs.md`](reviewing-prs.md) | Reviewing someone else's PR | Leftover-artifact findings follow what the PR is landing, owner scope vs the author's mechanical box, review-only is not ARDI, post every finding already in hand, and only work PRs the user opened or is assigned to, or that a repository workflow opened (a sweep's "every open PR" is not a scope grant; UCD-SERG/serodynamics 2026-09-01), split from `preferences.md` at the 1200-line gate (UCD-SERG/shigella#31). |
| [`github.md`](github.md) | GitHub PR/issue queue management | Operational write-action checklist, stale remotes, the GII startup sweep, stacked-PR pitfalls, consumer enumeration (`gh search code`), secondary rate limits, and `gh pr edit`. The `gh` CLI itself, GitLab, remote-session bash access, and consumer-CI content now live in their own split files (linked at the top). |
| [`gh-cli.md`](gh-cli.md) | GitHub CLI (`gh`) | The `gh` CLI behavior, rate limits, pagination, handling `GH_PAGER`, GraphQL vs REST pools, and fallback strategies. Split out of `github.md`. |
| [`github-remote-sessions.md`](github-remote-sessions.md) | GitHub access from bash in remote/web sessions | What a remote or web session can reach on GitHub from bash when `gh`/`glab` are absent: session-scope 403s, the MCP-then-`add_repo`-then-`git ls-remote` ladder, the proxy's push-but-not-delete rule, and GitHub Pages policy denials. Split out of `github.md` (ai-config#694 pattern) at the 1200-line gate. |
| [`github-mcp-tools.md`](github-mcp-tools.md) | GitHub MCP tools (Claude Code remote/web sessions) | The GitHub MCP tool surface used when `gh` is unavailable: tool selection, scope and owner-string quirks, review/comment/thread mechanics, and per-tool failure modes. Split out of `github.md` (ai-config#694 pattern) pre-emptively at 1199 lines, just under the 1200-line gate. |
| [`github-repo-transfers.md`](github-repo-transfers.md) | Repository transfers between owners | What a transfer does not carry (Actions secrets, so every secret-dependent workflow silently stops), which paths GitHub redirects (`pull` but not `issues`), and why `gh pr create` fails while `origin` still names the old owner. Split out of `github.md` (ai-config#694 pattern) at the 1200-line gate. |
| [`github-closing-keywords.md`](github-closing-keywords.md) | GitHub closing-keyword parser traps | `KEYWORD #N` matches as a substring, so a sentence that *says* the keyword is not being used still closes `#N`. Split out of `github.md` at the 1200-line gate. |
| [`gitlab.md`](gitlab.md) | GitLab CLI and Discussions API | `glab` and the GitLab Discussions API for inline MR comments. Split out of `github.md` (ai-config#694 pattern) at the 1200-line gate. |
| [`github-consumer-ci.md`](github-consumer-ci.md) | Consumer-side GitHub CI, App installs, and moving-tag pins | A documented-but-wrong convention whose real fix is already upstream, a GitHub App install check, and a moving major tag that turns the default branch red with no local change. Split out of `github.md` (ai-config#694 pattern) at the 1200-line gate. |
| [`github-actions.md`](github-actions.md) | GitHub Actions authoring | Workflow authoring gotchas, YAML quoting for Actions, gha's changelog conventions, and what a repo/org rename does to `uses:` refs. |
| [`gha-reusable-workflows.md`](gha-reusable-workflows.md) | `Morrison-Lab/gha` reusable workflows | The reusable-workflow catalog, tag-sliding rules, caller conventions, and consumer-side gotchas for `d-morrison/gha` / `Morrison-Lab/gha`. Split out of `github-actions.md` (ai-config#1680) at the 1200-line gate. |
| [`github-actions-outages.md`](github-actions-outages.md) | GitHub Actions platform outages, and their lookalikes | Detecting a live incident (absent rather than failing checks, the `githubstatus.com` JSON endpoints, the plural-versus-single tell), ruling one out when a shared-config change fails every repo at once instead (and building that timeline from merges rather than commit dates), and cleaning up after one (a swallowed push versus jobs cancelled mid-flight, and why zero recorded steps is the discriminator). Split out of `github-actions.md` (ai-config#694 pattern) at the 1200-line gate. |
| [`github-actions-secrets.md`](github-actions-secrets.md) | Actions secret scoping, and proving a credential works | Org-versus-repo secret precedence (a repo copy shadows the org one, so a migration is inert and unverifiable until the copies go), the `--visibility private` default, why a green job conclusion and a missing artifact both fail to show whether a credential authenticated (the execution result's `total_cost_usd` does), and keeping a token out of an agent's context. |
| [`gha-reusable-workflow-permissions.md`](gha-reusable-workflow-permissions.md) | Consumer-side gotchas calling `Morrison-Lab/gha`'s Claude workflows | A `permissions:` mismatch against a nested job fails with zero jobs and zero check runs (invisible to `gh pr checks`); a caller-level `concurrency:` block reusing a nested job's own group name deadlocks the run outright. Satellite of `gha-reusable-workflows.md`, kept as a separate file rather than merged in. |
| [`claude-bot-workflows.md`](claude-bot-workflows.md) | The `@claude` bot workflows | The bot's own behaviour once a run starts: the `@claude` CI action, `claude-code-action`'s tag vs. agent modes, gathering prior review context, disabling the agent in a consumer repo, a posted comment as a mention-trigger surface, and the token failures (`CLAUDE_CODE_OAUTH_TOKEN` identity, `pull_request_target`). |
| [`claude-review-dispatch.md`](claude-review-dispatch.md) | Dispatching a review, and posting its reply | How a review gets **triggered** per repo family and what becomes of the reply: re-triggering the PR review, the self-modification skip guard, how `ai-config`'s own auto-review trigger has flipped (with its dated trigger table and dispatch command), why a workflow that posts the *last* assistant message loses the reply when a corpus rule claims that slot, and why review workflows upload internal Actions artifacts while agents inspect PR comment payloads. Satellite of `claude-bot-workflows.md`, split at the 1200-line gate. |
| [`mention-triggers.md`](mention-triggers.md) | Triggering the bot by naming it | Why a documented rule kept recurring: the caller's `contains()` gate reads the raw body, so backticks, a code fence, a heading, and descriptive framing all leave a mention live; the existing Do/Don't bullets each scope that warning to their own incident's surface; and the recurrence was a placement problem, since every home for the rule was in `memories/` or a skill body while the auto-loaded fragment that prompts the offending comment carried no pointer. Satellite of `claude-bot-workflows.md`, which owns the mechanism and the five prior instances. |
| [`git.md`](git.md) | Git | Git itself: submodule pin bumps, scanning for parallel/in-flight work, PR-branch lookup and `switch -C` safety, merge-conflict mechanics (`merge --continue`, leftover markers, uncommitted edits riding along), Windows / Git Bash quirks (`core.fileMode`, MSYS path conversion), a silently-failed `checkout` continuing on the wrong branch, backtick spans running as shell commands in commit/PR-comment bodies, citing evidence in a PR's own superseded (and possibly squashed-away) commits, and a multi-repo hook naming a branch but not which checkout it looked at, plus a shallow clone making `git log -S`/`--follow`/`blame` report a graft as the introduction. |
| [`git-branches.md`](git-branches.md) | Git branches | Branch and remote-branch lifecycle: `gh pr merge --delete-branch` orphaning a stacked PR instead of retargeting it, a head-branch rename closing its own PR, rebuilding a stacked PR across a squash-merge (with the `stale info` recovery), pushing a stray branch instead of HEAD, the remote-session push-proxy deletion no-op / HTTP 403, pruning a `[gone]` local branch safely in a squash-merging repo, recovering a closed PR's branch from `refs/pull/N/head`, uncommitted leftovers on a merged branch as a rejected direction rather than unfinished work, and an orphaned `refs/remotes/<ns>/*` namespace inflating a branch count. Split out of `git.md` (ai-config#694 pattern) at the 1200-line gate. |
| [`git-diffing.md`](git-diffing.md) | Git diffing | Diff-range selection (`..` vs `...` vs the working tree) and diff-scoped check pitfalls (silently no-op-ing on an uncommitted or empty diff, blindness to untracked files, an untracked copy shadowing a tracked script), plus the pathspec-vs-glob and `for-each-ref`-vs-`ls-files` matcher mismatches and `git rev-parse <ref>:<path>`'s echo-the-input failure mode. Split out of `git.md` (ai-config#694 pattern) at the 1200-line gate. |
| [`git-stash.md`](git-stash.md) | Git stash | The stash stack: why `git stash` on an already-clean tree saves nothing, so a later `pop` restores another branch's leftover work, plus the recovery and the line-by-line supersession check to run before dropping an entry. Split out of `git.md` (ai-config#694 pattern) at the 1200-line gate. |
| [`git-tags.md`](git-tags.md) | Git tags | Tag management: force-moving / sliding tags, `git fetch --tags --force`, and peeling annotated tags to commit SHAs (`git ls-remote` two-refspec pattern, `awk` version comment extraction). Satellite of `git.md`, split at the 1200-line gate. |
| [`git-worktrees.md`](git-worktrees.md) | Git worktrees | Worktree-specific behavior: `worktree add`/`remove` gotchas, submodules inside a worktree, `checkout -B` bypassing the already-checked-out guard, pushing by refspec when a branch is checked out elsewhere, two worktrees sharing one branch ref, and the harness's own subagent worktree, which `isolation: "worktree"` places in the session's primary repo rather than in a repo the brief names. Split out of `git.md` (ai-config#694 pattern) at the 1200-line gate. |
| [`r-quarto.md`](r-quarto.md) | R & the R toolchain | `renv` (lockfiles, per-worktree libraries), the linters (`lintr`, `air`, `jarl`), R-package PR CI gates, testthat gotchas, and `WORDLIST` collation. Quarto *site* material now lives in `quarto-sites.md`. |
| [`quarto-sites.md`](quarto-sites.md) | Quarto site authoring & rendering | Project configuration, build and layout gotchas, page-scoped crossref labels, why a project's `format:` block is not the set of formats it renders, and the `quarto-actions/setup` + TinyTeX failure signatures. Split out of `r-quarto.md` at the 1200-line gate. |
| [`r-cloud-sessions.md`](r-cloud-sessions.md) | R & Quarto in Claude Code cloud / web sessions | Running R and Quarto inside a remote container: toolchain installation, missing binaries, sandbox constraints, and verifying what a PR preview actually built (split out of `r-quarto.md`). |
| [`python.md`](python.md) | Python and CPython runtime behavior | CPython constant folding hiding literals from top-level co_consts probes (thresholds, frozenset and nested-tuple shapes, the recursive-walk guard, PEP 709 inlining), and os.kill(pid, 0) on Windows being GenerateConsoleCtrlEvent --- success does not track liveness and nothing is delivered for a nonzero pid --- plus the zombie/open-handle caveats for wait-for-death loops, and Windows timezone limits: `time.tzset()` is Unix-only, `zoneinfo` without `tzdata` raises `ZoneInfoNotFoundError` (not `ModuleNotFoundError`), and fixed-offset fallbacks are wrong for DST zones. |
| [`claude-code.md`](claude-code.md) | Claude Code harness & agent tooling | The harness itself: `AskUserQuestion`, the Bash tool (zsh, cwd persistence), `WebFetch`, `Monitor`, custom subagents, `Workflow` `agent()`, `codex`, skill command blocks, and this repo's own memory-file structure. |
| [`claude-code-consumer-wiring.md`](claude-code-consumer-wiring.md) | Wiring ai-config into a consumer repo's Claude bots | The `bootstrap.sh` vs bot wiring, the committed `.claude/skills` symlink, `.gitignore` and `checkout-submodules` steps, and the shallow-clone bogus merge-base and history-query caveats. Split out of `claude-code.md` at the 1200-line gate. |
| [`claude-code-webfetch.md`](claude-code-webfetch.md) | Claude Code web fetching: what `WebFetch` returns, and where the real source lives | A rendered docs site can 403 while its source file serves fine from `raw.githubusercontent.com`, and `WebFetch` itself answers with a model-written summary rather than the page --- so an exact literal has to be read from the source, not quoted from the fetch. Split out of `claude-code.md` (ai-config#694 pattern) at the 1200-line gate. |
| [`claude-code-scheduling.md`](claude-code-scheduling.md) | Claude Code self-scheduling: wakeups, `send_later`, `CronCreate`, and Routines | Arming a check-in for later, and the ways that goes quiet: `ScheduleWakeup`'s `/loop` scoping and its silent non-firing in a plain local session, `CronCreate`'s session-only store losing a job before its fire time, and `update_trigger` needing a fresh `run_once_at` to re-arm a fired one-shot Routine. The running theme is that a creation receipt is not a guarantee. Split out of `claude-code.md` (ai-config#694 pattern) at the 1200-line gate. |
| [`claude-code-transcripts.md`](claude-code-transcripts.md) | Claude Code transcript records | What is on disk under `~/.claude/projects/*.jsonl` for anything reading a transcript: why `message.role == "user"` is a transport role rather than an authorship claim, why `origin.kind` is absent from almost every genuine turn and what its `unclassified` and `verifiedSlackHumanTurn` traps are, and the several record shapes carrying user prose -- including `tool_result`, where an `AskUserQuestion` answer exists and nowhere else. |
| [`claude-code-hooks.md`](claude-code-hooks.md) | Claude Code hooks: manifest, registration, and install failures | How hooks reach a machine and how that goes wrong: the native `hooks/hooks.json` schema versus the flat `install-hooks.py` view, the split between `install-hooks.py` (which binds) and a manual copy (which places, the plugin loader serving its hooks from the plugin root instead), double registration when the plugin is also enabled, and the two routes to a registered-but-absent hook that takes the whole `Bash` tool down. |
| [`hooks.md`](hooks.md) | Active hooks catalog & proactive compliance guide | Complete 47-hook catalog across `UserPromptSubmit`, `PreToolUse`, `Stop`, and detached timers, with trigger criteria, verification mechanisms, and actionable instructions for proactive agent compliance. |
| [`claude-code-settings.md`](claude-code-settings.md) | Claude Code settings scopes and `enabledPlugins` | How a setting present in more than one `settings.json` resolves: the managed > command line > local > project > user ladder, why the user scope cannot override a repo's checked-in settings, and what that means for enabling or disabling a plugin (the `.claude/settings.local.json` opt-out, precedence rather than union, managed force-enable, and `defaultEnabled`). Satellite of `claude-code.md`. |
| [`claude-code-permissions.md`](claude-code-permissions.md) | Claude Code permission-rule matching mechanics | The syntax and matcher mechanics behind `allow`/`ask`/`deny` rules: the built-in matcher is shell-operator aware and checks a chained Bash command segment by segment (not as one string), what a bare tool name means in allow vs. deny, the `//` absolute-path prefix a single leading `/` does not give you, `Tool(param:value)` rules being deny/ask-only and never matching an omitted parameter, and why recording a wrong failure mechanism is worse than recording none. Satellite of `claude-code.md`. |
| [`claude-code-context-pools.md`](claude-code-context-pools.md) | ai-config's three context-cost pools (always-loaded / on-demand / generated) | Which layer actually costs context per session, why splitting an always-loaded fragment saves nothing while splitting an on-demand memory file does, the measured always-loaded closure size and its 2026-07-31 context-limit overflow on one `@claude` workflow run, the lab-manual-shared-fragment restructuring constraint, and demoting content to on-demand via a plain markdown link instead of an `@`-import. Split out of `claude-code.md` (ai-config#694 pattern) at the 1200-line gate. |
| [`codex-cloud.md`](codex-cloud.md) | Codex Cloud workspace policy and GitHub integrations | Workspace-policy boundaries for alternatives to administrator-disabled Codex Cloud features, including why personal workspaces apply only to personally owned repositories. |
| [`databricks-hosted-llms.md`](databricks-hosted-llms.md) | Databricks-hosted LLMs: CLI and agent client configuration | Homebrew 6.x's tap-trust gate on `databricks/tap`, `~/.databrickscfg`'s per-auth-type storage (plaintext PAT vs. OS-keychain OAuth), Codex CLI 0.151.0+ hard-rejecting `wire_api = "chat"`, the tools-plus-`reasoning_effort` Responses-API constraint applying to any reasoning-tier GPT endpoint rather than a named few, and the ChatGPT desktop app sharing `config.toml` with Codex CLI while its own chat UI cannot select a custom provider. |
| [`reddit-access.md`](reddit-access.md) | Reddit access routes for local Claude Code sessions | Reddit is unreachable from every headless surface a local session has (WebFetch and Browser-pane client-side blocks, WebSearch's crawler blocked by Reddit, curl 403'd on TLS fingerprint, dead mirrors --- five routes with their signatures); route 1 is not a permission rule (deny/ask/allow checked empty in every scope) and is probably but not confirmably the WebFetch domain safety preflight, whose documented override `skipWebFetchPreflight: true` nobody has tested against Reddit; the working route is Claude in Chrome's in-page fetch, with the origin, `window`-state accumulation, 45s CDP-cap, single-Blob-export, and pacing mechanics. |
| [`cursor.md`](cursor.md) | Cursor agent & plugin behavior | `.cursorignore` sandbox writes and hidden worktrees, plugin vs `~/.cursor/skills` vs `~/.cursor/rules` vs `~/.claude/skills` as alternatives rather than stacked installs, not pointing the Cursor plugin `hooks` field at Claude `hooks/hooks.json`, and Cursor Cloud `Task` dispatching `adversarial-reviewer` (prefer a listed Claude model when the conductor is not Claude). Split out of `tools.md` (ai-config#694 pattern) when a Cursor UMS append crossed the 1200-line gate. |
| [`cursor-bugbot.md`](cursor-bugbot.md) | Cursor Bugbot (GitHub PR reviewer) | Dashboard Bugbot vs `cursor-code-review.yml`, Team-key `HTTP 401 Invalid Team API Key`, GitHub App install != coverage, and the PR-author account-mismatch on `bugbot run`. Split out of `cursor.md` so the IDE/plugin file stays about the editor. |
| [`community-watchlist.md`](community-watchlist.md) | Agentic-AI community watchlist (Reddit) | The standing target list for practice-mining / opposition-research passes: 22 subreddits with measured 2026-08 practice-signal yields (yield, not subscriber count, ranks them), from the issue #2041 orchestrated review; re-measure yields each pass. Companion of `reddit-access.md`, which carries the access mechanics. |
| [`vscode-copilot-byok.md`](vscode-copilot-byok.md) | VS Code's built-in Copilot Chat and core BYOK model registration | Why `code --list-extensions` cannot see the bundled `GitHub.copilot-chat`; BYOK model registration living in VS Code core, with the API key going to secret storage and only a reference to configuration (so no file edit can seed a key); the `chat.agentHost.byokModels.enabled` gate and its `false` default alongside the `claudeAgent`/`codexAgent` switches; and the personal-machine (core BYOK) versus work-machine (OAICopilot extension) route split. Companion of the `register-oaicopilot-models` skill. |
| [`vscode-settings-sync.md`](vscode-settings-sync.md) | VS Code Settings Sync silently reverting a machine's config | The 2026-08-26 incident where sync replaced macOS `settings.json` wholesale from the work Windows machine's copy (31 of 37 `oaicopilot.models` context values flattened); the two identifying reads (sync-record mtime, CRLF line endings), the escaped-payload grep trap, attributing reverted values to a published table, and the pause-sync-then-backup repair procedure. Companion of `vscode-copilot-byok.md`. |
| [`vscode-profiles.md`](vscode-profiles.md) | VS Code profiles and extension isolation | Named profiles as persistent repo-specific extension allowlists, including the already-open-folder reuse trap and verification through the window title and extension-host logs. |
| [`tools.md`](tools.md) | Local tools & CLIs | The cross-cutting remainder that fits none of the topical files above: Julia in cloud sessions, `markdownlint`, editing committed Office Open XML files, personal machine setup, and a few standing writing / review habits. |
| [`wsl.md`](wsl.md) | WSL platform quirks | Running agent tooling from WSL on this machine: `npx` resolving to Windows node (CMD.EXE UNC failure, exit 2) and the standalone-Linux-node PATH recipe that fixes it. Satellite of `tools.md`, split at the 1200-line gate. |
| [`opencode-bash-windows.md`](opencode-bash-windows.md) | opencode's Bash tool on Windows | The intermittent ChildProcess.kill failure (#2077), its measured correlation with long/timeout-killed commands rather than chaining, the reliable `cmd /c` dodge, and four PowerShell-5.1 quoting/redirect traps (`&&` rejection, stolen `>nul`, leading-pipe `--jq`, embedded-quote loss across cmd). Satellite of `tools.md`, split at the 1200-line gate. |
| [`zsh.md`](zsh.md) | zsh semantics that produce a false absence | The zsh-versus-bash differences where a check reports nothing and looks like it ran: a process substitution feeding a pipeline (fd lifetime, reads as a clean zero), and an unmatched glob under `NOMATCH` skipping the simple command it appears in (so a multi-path existence check never lists the path that exists, the command's own `2>/dev/null` does not hide the error but a wrapper's does, and the glob's exit status survives `&&` while `;` replaces it). Carries the platform caveats: the process-substitution entry is zsh-on-Linux while `NOMATCH` is a documented zsh default, and BSD `ls` returns 1 where GNU coreutils returns 2 for an inaccessible command-line argument, so the durable tell is an empty stdout where a real path was requested rather than any exit code. Also why `NULL_GLOB` and `(N)` trade the false absence for a false presence while `CSH_NULL_GLOB` does not fix it at all. Satellite of `tools.md`, split at the 1200-line gate. |
| [`mistake-patterns.md`](mistake-patterns.md) | Recurring mistake patterns & failure recovery | Index of common failure modes across agent sessions (premature assumption, passivity on standing rules, giving up on missing executables), with cross-references to deep mechanisms in `preferences.md`, `growth-mindset.md`, and `CLAUDE.md`. |
| [`debugging.md`](debugging.md) | Debugging notes | Debugging practices: real-browser CSS/JS testing (not DOM stubs), VS Code editor-vs-disk desync, ARDI review-polling, portable bash scripting (sed, EOF, robustness), R test/lint CI-only gotchas and snapshot regeneration, and recovery from merge-clobbered or force-pushed PR branches. |
| [`cli-test-main.md`](cli-test-main.md) | CLI tests must invoke `main()` | Helper-only imports stay green when `main()` wires a skip-reason gate wrong. Satellite of `debugging.md`, split at the 1200-line gate recording #2292. |
| [`session-2026-08-22-gi-1953.md`](session-2026-08-22-gi-1953.md) | Session log: gi #1953 (merged via parallel session) | The #1953 run's outcome (PR #1976 squash-merged 74ed69d5; parallel-session takeover), the opencode permission findings, and issues #2061/#2077. Candidate for pruning once its content is captured elsewhere. |
| [`session-2026-08-23-gi-2082.md`](session-2026-08-23-gi-2082.md) | Session log: gi #2082 Windows PR-monitor backstop | The #2084 run through merge (a569d205) and live install on this host, the schtasks design, and follow-ups (#2098 test-runner hang, #2106 tmp litter). |
| [`verify-lever-before-change.md`](verify-lever-before-change.md) | Confirm the lever before building | Split from preferences.md: confirm the mechanism you are about to disable/change is the one producing what the user actually saw, before implementing; empty results are a question, not a go-ahead. |
| [`antigravity.md`](antigravity.md) | Google Antigravity & `agy` CLI | Hook lifecycle architecture (`PreToolUse`, `Stop`, `PreInvocation`), payload schema translation to Claude hooks, the `${extensionPath}` command-path requirement, `agy` CLI model selection, and non-interactive `-p` prompt gotchas. |
| [`delegation.md`](delegation.md) | CLI delegation ladder | Cost-first order, usage windows, agy CLI vs API, headless dispatch mechanics. |
| [`ai-writing-patterns.md`](ai-writing-patterns.md) | AI writing patterns, detection metrics, and anti-slop architectures | Empirical synthesis of 20 core AI writing patterns (corporate verb inflation, copula avoidance, negative parallelism, participle commentary, deletion test), statistical detection metrics (burstiness, TTR), and multi-tier enforcement architectures (session-start rules, on-demand skill catalogs, deterministic post-tool hooks). |
| [`benchbook-agent-knowledge-systems.md`](benchbook-agent-knowledge-systems.md) | Agent-curated knowledge bases & anti-bloat discipline (`benchbook`) | Failure modes of LLM knowledge systems (bloat over rot), the empirical anti-bloat rules (no `update` log operation, pointer-only todo lists, calibrated word count thresholds), compounding persistent wiki architecture vs. ephemeral RAG/transcripts, mechanical vs. judgment lint gates, and satellite contract patterns from `Ulef1005/benchbook`. |
